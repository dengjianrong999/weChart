<view>
<view class='songSearch'>
  <input class='search' placeholder='搜索' bindinput="getCode"></input>
</view>
  <view wx:for="{{list}}" wx:key="{{index}}" class='listImg'>
    <image src='{{item.album.artist.img1v1Url}}'></image>
    <text>{{item.name}}</text>
  </view>
</view>